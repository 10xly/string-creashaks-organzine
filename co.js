const creashaks = require('string-creashaks')
const space = require('space-string')
const organzine = require('string-organzine')

const concat = require('general-concat')

const CREASHAKS_ORGANZINE_STRING = concat(creashaks, space, organzine)

module.exports = CREASHAKS_ORGANZINE_STRING
